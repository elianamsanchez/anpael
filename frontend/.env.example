# Copiar a .env.local (que esta en .gitignore).
#
# Vacio = usar el proxy de Vite, que es lo recomendado en desarrollo.
# Con valor = axios pega directo a esa URL, y ahi si interviene CORS.
VITE_API_URL=
